# macOS
.DS_Store

# Claude Code local config
.claude/

# Supabase CLI local link state (machine-specific, not source)
supabase/.temp/
supabase/.branches/

# Secrets — never commit. Contains Supabase service_role key etc.
.env
.env.*
!.env.example
